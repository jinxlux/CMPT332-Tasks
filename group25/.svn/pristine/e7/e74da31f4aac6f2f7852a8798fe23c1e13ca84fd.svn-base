/* NAME: Xudong Li
   NSID:xul395
   Student Number:11206927
*/

/* NAME:Tong Wang   
   NSID: tow087 
   Student Number: 11201222
   CMPT 332 Term 1 2019
   
   group: 25
*/


#include "list.h"
#include <rtthreads.h>
#include <sys/types.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <sys/ioctl.h>
#include <stdlib.h>
#include <stdio.h>
#include <net/if.h>
#include <unistd.h>
#include <fcntl.h>
#include <netdb.h>
#include <string.h>

/*Header file was not asked for, so defining function prototypes here*/
RTTTHREAD keyboardThread();

RTTTHREAD printToScreen();

RTTTHREAD serverThread();

RTTTHREAD receiveNetworkMsg();

RTTTHREAD sendOverNetwork();


/*
 * Waits for input from keyboard
 * Once receives inputs, adds the message to a sender qeueue, notifys server thread
*/
RTTTHREAD keyboardThread(){
    printf("keyboardThread function need to be completed\n");
    return ;
}
/*
 * print information to screen when get the signal
 * 
*/
RTTTHREAD printToScreen(){
    printf("printToScreen function need to be completed\n");
    return ;    
}
/*
 * server thread to communicate with other 
 * 
*/
RTTTHREAD serverThread(){
    printf("serverThread function need to be completed\n");
    return ;    
}

/*
 * Waits for message(s) from machines over the network
 * 
*/
RTTTHREAD receiveNetworkMsg(){
    printf("receiveNetworkMsg function need to be completed\n");
    return ;
    
}

/*
 * Sends message(s) to machines over the network
 * 
*/
RTTTHREAD sendOverNetwork(){
    printf("sendOverNetwork function need to be completed\n");
    return ;
}

int mainp(int argc, char **argv){
    keyboardThread();

    printToScreen();

    serverThread();

    receiveNetworkMsg();

    sendOverNetwork();
    printf("mainp function need to be completed\n");
    return 0;
}